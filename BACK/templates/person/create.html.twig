{# templates/person/create.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer une Person</h1>

    {{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.poste) }}
    {{ form_row(form.equipe) }}
    {{ form_row(form.agence) }}
    {{ form_row(form.photo_pro) }}
    {{ form_row(form.photo_fun) }}
    {{ form_end(form) }}
{% endblock %}
